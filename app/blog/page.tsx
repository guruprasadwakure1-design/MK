import {Card,Section} from '@/components/ui';
export const metadata={title:'Blog'};const posts=[['Design validation for wear products','FEA, Validation','6 min'],['Building an innovation pipeline','Leadership, R&D','5 min']];export default function Page(){return <Section title="Engineering blog" eyebrow="Markdown content"><div className="grid gap-6 md:grid-cols-2">{posts.map(([t,tag,time])=><Card key={t}><p className="text-sm text-orange">{tag} • {time}</p><h3 className="mt-2 text-2xl font-bold">{t}</h3><p className="mt-3">Markdown-based article placeholder with tags, categories, search, reading time and syntax highlighting support.</p></Card>)}</div></Section>}
